/*
const testDate = (str) => {
  let o = [{
    date: str
  }]
  parseDate(o)
  if (typeof o[0].date === 'object') {
    console.log(`[${str}] converted to [${o[0].date}]`)
  }
}

let example = [
  '20 Jan',
  '30',
  'Thurs, 20th Jan'
]

for (let i = 0; i < example.length; i++) {
  testDate(example[i])
}
*/
